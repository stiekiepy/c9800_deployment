!SHUTDOWN RADIOS TO SKIP DA ZERO
ap country US
ap dot11 5ghz shutdown
ap dot11 24ghz shutdown


hostname US-STIAN-P-WC-CTL-001-IT

int gi0
ip add 10.0.0.10 255.255.255.0
exit
ip route vrf Mgmt-intf 0.0.0.0 0.0.0.0 10.0.0.1

no ip domain lookup

username wlcadmin privilege 15 password 0 wlcpassword

ip domain name stianliebenberg.com
crypto key generate rsa modulus 2048
ip ssh version 2


line con 0
privilege level 15
!login authentication CONSOLE

line vty 0 15
transport input ssh

line vty 16 50
transport input ssh

cdp run

service timestamps debug datetime msec
service timestamps log datetime msec
service tcp-keepalives-in
service tcp-keepalives-out
logging buffered 512000